작업환경 : Linux

컴파일 방법 : 20141603.c, 20141603.h 가 같은 디렉터리에 존재하고 ,opcode.txt 파일 역시 존재한다면, gcc 컴파일러를 이용하여 컴파일 하고 ./a.out 파일을 만들어서 실행할 수 있다.

실행방법 :(1) Linux 창에서 tar 압축을 푼다. 
          (2) make를 입력하여 20141603.out이라는 실행 파일을 만든다
          (3) ./20141603.out을  입력하여 실행시킨다.
